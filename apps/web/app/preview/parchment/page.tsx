import { Home } from '@/components/home';

export const metadata = { title: 'Parchment — direction preview', robots: { index: false } };

export default function ParchmentPreviewPage() {
  return <Home />;
}
